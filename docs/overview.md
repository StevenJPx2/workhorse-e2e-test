# Toolkit Overview

A dependency-free TypeScript utility library. Modules live under `src/`, each
grouping related helpers. The public surface is re-exported from `src/index.ts`.

See each module's source for the exported functions.
